pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
               
               sshagent(['${credos}']) { 
                   sh '''
                   echo "ok"
                   '''
               }
            }
        }
    }
}

